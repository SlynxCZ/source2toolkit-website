---
title: IToolkitMySQL
---

# IToolkitMySQL

## IToolkitMySQLResult

### int GetRowCount()

Returns the total number of rows in the result set.

### int GetFieldCount()

Returns the number of columns in the result set.

### bool FieldNameToNum(name, columnId)

Converts a field name into a column index.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="name" type="const char*" /> — Field name to search for.
- <ApiParam name="columnId" type="unsigned int*" /> — Output column index.

### const char* FieldNumToName(colId)

Returns the field name for a column index.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="colId" type="unsigned int" /> — Column index.

### bool MoreRows()

Checks whether additional rows are available.

### IToolkitMySQLRow* FetchRow()

Fetches the next row from the result set.

### IToolkitMySQLRow* CurrentRow()

Returns the currently active row.

### bool Rewind()

Rewinds the internal row cursor to the beginning.

### int GetFieldType(field)

Returns the MySQL field type of a column.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="field" type="unsigned int" /> — Column index.

### const char* GetString(columnId, length)

Retrieves a string value from a column.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.
- <ApiParam name="length" type="size_t*" /> — Optional output string length.

### size_t GetDataSize(columnId)

Returns the raw data size of a column value.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.

### float GetFloat(columnId)

Retrieves a floating-point value from a column.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.

### int GetInt(columnId)

Retrieves an integer value from a column.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.

### int64_t GetInt64(columnId)

Retrieves a 64-bit integer value from a column.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.

### bool IsNull(columnId)

Checks whether a column contains a NULL value.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="columnId" type="unsigned int" /> — Column index.

## IToolkitMySQLQuery

### IToolkitMySQLResult* GetResultSet()

Returns the current result set.

### bool FetchMoreResults()

Advances to the next available result set.

### unsigned int GetInsertId()

Returns the last inserted row ID.

### unsigned int GetAffectedRows()

Returns the number of affected rows.

## IToolkitMySQLConnection

### Connect(callback)

Establishes a connection to the MySQL server.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="callback" type="ToolkitMySQLConnectCallbackFunc" /> — Callback invoked after connection attempt.

### Query(query, callback)

Executes a raw SQL query.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="query" type="char*" /> — SQL query string.
- <ApiParam name="callback" type="ToolkitMySQLQueryCallbackFunc" /> — Callback invoked with query result.

### Query(query, callback, )

Executes a formatted SQL query.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="query" type="const char*" /> — SQL format string.
- <ApiParam name="callback" type="ToolkitMySQLQueryCallbackFunc" /> — Callback invoked with query result.
- <ApiParam type="..." />

### ExecuteTransaction(txn, success, failure)

Executes multiple queries as a transaction.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="txn" type="ToolkitMySQLTransaction" /> — Transaction query batch.
- <ApiParam name="success" type="ToolkitMySQLTransactionSuccessCallbackFunc" /> — Callback invoked on success.
- <ApiParam name="failure" type="ToolkitMySQLTransactionFailureCallbackFunc" /> — Callback invoked on failure.

### Destroy()

Destroys and releases the connection instance.

### std::string Escape(string)

Escapes a mutable string for safe SQL usage.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="string" type="char*" /> — Input string.

### std::string Escape(string)

Escapes a constant string for safe SQL usage.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="string" type="const char*" /> — Input string.

## IToolkitMySQL

### IToolkitMySQLConnection* CreateConnection(info)

Creates a new MySQL connection instance.

<ApiLabel>Parameters</ApiLabel>

- <ApiParam name="info" type="ToolkitMySQLConnectionInfo" /> — Connection configuration data.

